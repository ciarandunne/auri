services:
  auri:
    build: .
    container_name: auri
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      AURI_DB_PATH: /app/data/auri.db
      TZ: America/New_York
    restart: unless-stopped
