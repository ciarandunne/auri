{
  "name": "auri",
  "version": "0.1.0",
  "description": "A tiny local scan receiver for a DIY NFC/RFID music box.",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "seed:dev": "node --disable-warning=ExperimentalWarning scripts/seed-local-dev-db.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "esphome-native-api": "^1.0.9"
  }
}
